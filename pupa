import * as std from "std";

let pupa = request => {
  request = request.split("\n");
  request.forEach( line => {
    if(/lead/i.test(line.split(":")[0])) {
      eval(line.substr(5)); 
    }
  });
};

export default pupa;
